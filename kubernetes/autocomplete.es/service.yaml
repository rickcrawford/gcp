apiVersion: v1
kind: Service
metadata:
  name: autocomplete
  labels:
    component: autocomplete
spec:
  type: NodePort
  selector:
    component: autocomplete
  ports:
  - name: http
    port: 80
    protocol: TCP
  - name: https
    port: 443
    protocol: TCP
 
